@* Proveedores de MudBlazor con el tema verde. Colocar una sola vez en el layout raíz de cada app. *@
<MudThemeProvider Theme="_tema" />
<MudPopoverProvider />
<MudDialogProvider CloseOnEscapeKey="true" BackdropClick="false" FullWidth="true" MaxWidth="MaxWidth.Small" />
<MudSnackbarProvider />

@code {
    private readonly MudTheme _tema = CgPosTheme.Crear();
}
